import { useState } from 'react'
import { Camera, CheckCircle2, LoaderCircle, Upload } from 'lucide-react'
export default function Diagnosis(){const [state,setState]=useState('idle'); const analyze=()=>{setState('loading');setTimeout(()=>setState('done'),1200)};return <section className="diagnosis panel"><div><p className="eyebrow">NEW DIAGNOSIS</p><h2>What are you seeing?</h2><p className="muted">Capture a clear crop image. Fasalyn checks visual symptoms against the current farm context.</p></div><div className="upload" onClick={analyze}>{state==='loading'?<LoaderCircle className="spin"/>:state==='done'?<CheckCircle2/>:<><Camera/><Upload size={16}/></>}<span>{state==='loading'?'Analyzing crop…':state==='done'?'Likely thrips damage detected — 91% confidence':'Add a crop photo'}</span></div>{state==='done'&&<div className="result"><b>Suggested next step</b><span>Record a trap observation for P5 to update its risk forecast.</span></div>}</section>}
