import {useEffect,useState} from 'react';import {PageTitle,Empty,RiskBadge} from '../components/Shared';import {getRisk} from '../services/api'
export default function Risk(){const plotId=localStorage.getItem('fasalyn-default-plot');const[risk,setRisk]=useState(null),[error,setError]=useState('');useEffect(()=>{if(plotId)getRisk(plotId).then(setRisk).catch(e=>setError(e.message))},[plotId]);if(!plotId)return <><PageTitle eyebrow="RISK ENGINE" title="Risk forecast"/><Empty title="Create a plot first" body="Risk requires a real persisted plot and observations."/></>;if(error)return <><PageTitle eyebrow="RISK ENGINE" title="Risk forecast"/><p className="form-error">{error}</p></>;if(!risk)return <><PageTitle eyebrow="RISK ENGINE" title="Risk forecast"/><p>Calculating from available farm signals…</p></>;return <><PageTitle eyebrow="EXPLAINABLE RISK ENGINE" title="Risk forecast"/><section className="risk-hero card"><div><p className="eyebrow">CURRENT ASSESSMENT</p><h2>{risk.level==='INSUFFICIENT_DATA'?'Insufficient data':`${risk.level} risk`}</h2><p>{risk.forecast?.statement||'Add observations, model output, or weather-enabled farm coordinates.'}</p><RiskBadge risk={risk.level}/></div><div className="risk-score"><b>{risk.score??'—'}</b><span>{risk.score==null?'more data needed':'/100 risk estimate'}</span></div><div className="factor-list"><h3>Contributing factors</h3>{risk.factors.map(f=><span key={f.signal}>{f.detail}<b>+{f.points}</b></span>)}{risk.missing?.map(x=><span key={x}>{x} unavailable <b>—</b></span>)}</div></section>{risk.weather?<section className="card forecast-page"><h2>Weather input</h2><p>{risk.weather.provider} · {risk.weather.temperature}°C · {risk.weather.humidity}% humidity · observed {risk.weather.observedAt}</p></section>:<p className="title-subtitle">Weather data unavailable. It was not used in this assessment.</p>}</>}
