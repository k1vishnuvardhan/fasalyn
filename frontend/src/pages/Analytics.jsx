import {useEffect,useState} from 'react';import {PageTitle,Empty} from '../components/Shared';import {getAnalytics} from '../services/api'
function Series({title,items,label}){return <article className="card analytic-card"><h2>{title}</h2>{!items.length?<p>No persisted data yet.</p>:items.map(x=><div className="timeline" key={x.day}><i/><div><b>{x.count}</b><span>{label}</span></div><small>{x.day}</small></div>)}</article>}
export default function Analytics(){const[data,setData]=useState(null),[error,setError]=useState('');useEffect(()=>{getAnalytics().then(setData).catch(e=>setError(e.message))},[]);return <><PageTitle eyebrow="PERSISTED FARM DATA" title="Farm analytics"/>{error&&<Empty title="Sign in to view analytics" body={error}/>} {!data&&!error&&<p>Loading analytics…</p>}{data&&<section className="analytics-grid"><Series title="Completed scans" items={data.scans} label="completed scans"/><Series title="Pest trap counts" items={data.traps} label="total recorded pests"/></section>}</>}
