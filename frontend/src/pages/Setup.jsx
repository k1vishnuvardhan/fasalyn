import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {PageTitle} from '../components/Shared';import {createFarm,createPlot,registerAccount} from '../services/api'
export default function Setup(){const[form,setForm]=useState({name:'',email:'',password:'',farm:'',plot:'',crop:'Chilli',latitude:'',longitude:''}),[error,setError]=useState(''),[busy,setBusy]=useState(false);const nav=useNavigate();const set=(key,value)=>setForm(x=>({...x,[key]:value}));const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const account=await registerAccount({name:form.name,email:form.email,password:form.password});localStorage.setItem('fasalyn-token',account.token);localStorage.setItem('fasalyn-user',JSON.stringify(account.user));const farm=await createFarm({name:form.farm,latitude:form.latitude===''?undefined:Number(form.latitude),longitude:form.longitude===''?undefined:Number(form.longitude)});const plot=await createPlot(farm.id,{name:form.plot,crop:form.crop});localStorage.setItem('fasalyn-default-plot',plot.id);nav('/scanner')}catch(err){setError(err.message)}finally{setBusy(false)}};return <><PageTitle eyebrow="GET STARTED" title="Create your first farm"/><form className="card observation" onSubmit={submit}><label>Your name<input required value={form.name} onChange={e=>set('name',e.target.value)}/></label><label>Email<input required type="email" value={form.email} onChange={e=>set('email',e.target.value)}/></label><label>Password<input required minLength="12" type="password" value={form.password} onChange={e=>set('password',e.target.value)}/></label><label>Farm name<input required value={form.farm} onChange={e=>set('farm',e.target.value)}/></label><label>Plot name<input required value={form.plot} onChange={e=>set('plot',e.target.value)}/></label><label>Crop<select value={form.crop} onChange={e=>set('crop',e.target.value)}><option>Chilli</option><option>Tomato</option><option>Rice</option><option>Cotton</option><option>Maize</option></select></label><label>Latitude (optional; enables real weather)<input type="number" step="any" value={form.latitude} onChange={e=>set('latitude',e.target.value)}/></label><label>Longitude (optional; enables real weather)<input type="number" step="any" value={form.longitude} onChange={e=>set('longitude',e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<button disabled={busy} className="primary-action">{busy?'Creating…':'Create farm and continue'}</button></form></>}
