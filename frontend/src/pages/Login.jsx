import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {PageTitle} from '../components/Shared';import {loginAccount} from '../services/api'
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');const nav=useNavigate();const submit=async e=>{e.preventDefault();try{const result=await loginAccount({email,password});localStorage.setItem('fasalyn-token',result.token);localStorage.setItem('fasalyn-user',JSON.stringify(result.user));nav('/dashboard')}catch(err){setError(err.message)}};return <><PageTitle eyebrow="ACCOUNT" title="Sign in"/><form className="card observation" onSubmit={submit}><label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input required type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<button className="primary-action">Sign in</button><p>New farmer? <Link to="/setup">Create a farm account</Link></p></form></>}
