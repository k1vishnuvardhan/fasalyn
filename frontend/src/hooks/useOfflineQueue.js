import {useEffect,useState} from 'react';import {clearQueue,queueItem,queuedItems} from '../offline/queue'
export function useOfflineQueue(){const [items,setItems]=useState([]),[online,setOnline]=useState(navigator.onLine);const refresh=()=>queuedItems().then(setItems).catch(()=>setItems([]));useEffect(()=>{refresh();const on=()=>setOnline(navigator.onLine);addEventListener('online',on);addEventListener('offline',on);return()=>{removeEventListener('online',on);removeEventListener('offline',on)}},[]);return{items,online,add:async(type,payload)=>{await queueItem(type,payload);refresh()},sync:async()=>{if(!online)return;await clearQueue();refresh()}}}
